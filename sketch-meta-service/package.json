{
  "name": "meta-automation-poc",
  "version": "1.0.0",
  "description": "POC for Meta Sketch Automation",
  "scripts": {
    "dev": "npm run build --prefix backend && npm run serve --prefix backend",
    "dev:debug": "set PLAYWRIGHT_HEADLESS=false&& npm run build --prefix backend && npm run serve --prefix backend",
    "start:firestore": "firebase emulators:start --only firestore --project meta-automation-poc",
    "install:all": "npm install && cd backend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "firebase-tools": "^13.0.0"
  }
}
