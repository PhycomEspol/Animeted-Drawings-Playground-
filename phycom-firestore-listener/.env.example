# Firestore Emulator connection
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080

# Project ID (must match the emulator's project)
GCLOUD_PROJECT=meta-automation-poc

# Idempotent mode: prevents duplicate processing across restarts
IDEMPOTENT_MODE=true

# HTTP API port
PORT=4001
